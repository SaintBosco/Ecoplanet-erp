# Deploy env — copy to deploy/.env on the VPS, fill in the real value, never commit it.
POSTGRES_PASSWORD=

# Comma-separated. Set to the real https:// domain once chosen (see ROADMAP.md).
ALLOWED_ORIGINS=http://localhost:3001
